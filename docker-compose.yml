services:
  api:
    build:
      context: ./backend
      dockerfile: dockerfile
    ports:
      - "5000:5000"

    volumes:
      - ./backend/:/backend/
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DJANGO_ENV=development