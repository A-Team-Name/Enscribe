services:
  api:
    build:
      context: ./backend
      dockerfile: dockerfile
    ports:
      - "5000:5000"
  # client:
  #   build:
  #     context: ./frontend
  #     dockerfile: dockerfile
  #   ports:
  #     "3000:3000"